[pytest]
# Look for tests only in tests/ (not in the check_*.py scripts).
testpaths = tests
# Let tests import clean.py and metrics.py from the project folder, and the evaluation set from eval/.
pythonpath = . eval
